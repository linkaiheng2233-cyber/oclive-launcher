# 复制为 .env 或 .env.local 后生效（仅影响前端构建时注入的常量）

# 设为 true 时：「新手」页顶部的「维护者通知」显示为可编辑文本框并可保存。
# 发给最终用户的安装包请勿开启此项，这样他人只能阅读通知、无法在界面里修改。
# 维护者仍可编辑应用配置目录下的 announcements.md。
VITE_ANNOUNCE_EDITABLE=false

# 「版本」菜单里「网站上看版本列表」打开的地址（OCLive 市场 /versions）。自定义域名时改成你的完整 URL。
VITE_VERSIONS_PAGE_URL=https://linkaiheng2233-cyber.github.io/oclive-plugin-market/versions
